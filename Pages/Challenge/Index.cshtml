@page
@model ByteBound.Pages.Challenge.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Challenges[0].ChallengeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Challenges[0].ChallengeAnswer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Challenges[0].ReleaseDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Challenges[0].Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Challenges[0].Points)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Challenges) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ChallengeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ChallengeAnswer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReleaseDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Points)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
